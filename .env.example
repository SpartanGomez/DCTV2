# Server port (default 8080 per SPEC §5)
PORT=8080

# Log level: silent | error | warn | info | debug
LOG_LEVEL=info

# Environment. Dev cheats and seed overrides are only accepted when NODE_ENV !== 'production'.
NODE_ENV=development
